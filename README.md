The built-in objects and methods include the Date object, along with its getDate(), getMonth(), and getFullYear() methods in part 1, the Number object with the methods Number(), Number.isInteger(), Number.isNaN(), and toLocaleString(),  the String object to use the replace method, and the getElementById() method.
https://bobble222.github.io/comp484-hw9/
By far the easiest was part 4, as I realized once I got there was that i had already used 2 if-else statements in part 1, and the hardest was definitely part 3 as I tried to figure out where and how to use each method, and understand why some didn't work exactly as expected. I learned that the date and number objects have many different methods to solve many questions that you might want to do with their respective objects, but Date is far more straightforward than Number is. Displaying results in the browser through javascript took a moment to adjust to, but it seems fairly simple once you get a good grasp of it!
